@page
@model ControlSeguros.App.Frontend.Pages.JefeOp.ListaJefeOp
@{
}

<h2>Lista de Jefe De Operaciones</h2>

<a asp-page="./CrearJefeOp" class="btn btn-primary">Registrar</a>

<br><br>

<form method="get">
    <div class="form-group">
        <p>
            Jefe de Operaciones: <input type="text" name="filtroBusqueda" asp-for="FiltroBusqueda"/>
            <input type="submit" value="Filtrar" class="btn btn-primary"/>
        </p>
    </div>
</form>

<table class="table mt-5">
    <thead>
        <tr>
            <th scope="col">Documento</th>
            <th scope="col">Nombre</th>
            <th scope="col">Apellidos</th>
            <th scope="col">Teléfono</th>
            <th scope="col">Dirección</th>
            <th scope="col"></th>
            
        </tr>
    </thead>
    @foreach (var JefeOp in Model.ListaJefeOp)
    {
        <tr>
            <td>@JefeOp.Documento</td>
            <td>@JefeOp.Nombre</td>
            <td>@JefeOp.Apellidos</td>
            <td>@JefeOp.Telefono</td>
            <td>@JefeOp.Direccion</td>
            <td>
                <a class="btn btn-primary table-btn" asp-page="./EditarAuxiliar" asp-route-MecanicoId="@JefeOp.JefeOpId">
                    <i class="fas fa-address-book fa-lg" aria-hidden="true"> </i>
                    Editar
                </a>
            </td>
            <td>
                <form method="post" asp-page-handler="Delete" asp-route-id="@JefeOp.JefeOpId">
                    <button class="btn btn-danger">Eliminar</button>
                </form>
            </td>
        </tr>
    }
</table>